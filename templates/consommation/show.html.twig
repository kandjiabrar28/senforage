{% extends 'base.html.twig' %}

{% block title %}Consommation{% endblock %}

{% block body %}
    <h1>Consommation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consommation.id }}</td>
            </tr>
            <tr>
                <th>Nouvelindex</th>
                <td>{{ consommation.nouvelindex }}</td>
            </tr>
            <tr>
                <th>Ancienindex</th>
                <td>{{ consommation.ancienindex }}</td>
            </tr>
            <tr>
                <th>Datereleve</th>
                <td>{{ consommation.datereleve ? consommation.datereleve|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consommation_index') }}">back to list</a>

    <a href="{{ path('consommation_edit', {'id': consommation.id}) }}">edit</a>

    {{ include('consommation/_delete_form.html.twig') }}
{% endblock %}
