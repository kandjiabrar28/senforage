{% extends 'base.html.twig' %}

{% block title %}Compteur{% endblock %}

{% block body %}
    <h1>Compteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compteur.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ compteur.numero }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ compteur.etat ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('compteur_index') }}">back to list</a>

    <a href="{{ path('compteur_edit', {'id': compteur.id}) }}">edit</a>

    {{ include('compteur/_delete_form.html.twig') }}
{% endblock %}
