{% extends 'base.html.twig' %}

{% block title %}Chefvillage{% endblock %}

{% block body %}
    <h1>Chefvillage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chefvillage.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ chefvillage.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ chefvillage.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ chefvillage.adresse }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ chefvillage.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chefvillage_index') }}">back to list</a>

    <a href="{{ path('chefvillage_edit', {'id': chefvillage.id}) }}">edit</a>

    {{ include('chefvillage/_delete_form.html.twig') }}
{% endblock %}
